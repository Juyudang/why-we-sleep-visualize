# 수면압력 시각화 정리 — 수치와 화면을 왜 이렇게 바꿨나

Visual Note 01(`sleep_pressure_sim/` + `app.js`)을 손보면서 알아낸 것과 정한 값을 남긴다.
"숫자가 현실적이지 않다"와 "잘 안 보인다" 두 가지가 출발점이었고, 각각 원인이 따로 있었다.

---

## 1. 무엇이 잘못돼 있었나

### 1-1. 기본값으로 하루가 성립하지 않았다

고치기 전 모델을 그대로 파이썬으로 돌려 72시간을 재현했더니 이런 하루가 나왔다.

| 잠든 시각 | 기상 시각 | 길이 |
|---|---|---|
| 18:48 | 20:00 | 1.2시간 |
| 23:18 | 01:42 | 2.4시간 |
| 17:00 | 17:54 | 0.9시간 |
| 21:24 | 23:12 | 1.8시간 |
| 03:54 | 05:24 | 1.5시간 |
| 18:18 | 19:18 | 1.0시간 |
| 22:36 | 00:48 | 2.2시간 |

사흘에 일곱 번, 매번 1~2시간씩, 저녁 5시에도 잠들었다.
화면에서는 얇은 띠 일곱 개로 보여서 "잠을 잤다"는 느낌 자체가 안 났다.

원인은 셋이었다.

1. **자는 동안 아데노신이 너무 빨리 빠졌다.** 감소율 0.72/h는 시상수 1.4시간이다.
   1시간만 자도 절반이 사라지니 곧바로 기상 조건에 걸렸다.
   실제 값은 4.2시간이고, 그래야 8시간을 자도 15%가 남는다.
2. **하루주기 위상이 6시간쯤 어긋나 있었다.** `0.42 + 0.3·sin(...)`에 기본 위상을 넣으면
   각성 신호가 **오후 2시에 최고, 새벽 2시에 최저**가 된다.
   실제 각성 리듬은 저녁 9시 무렵이 정점(각성 유지 구간)이고 새벽 4~5시가 최저다.
3. **임계값·상승률·위상이 서로 아귀가 안 맞았다.** 세 값이 각각 그럴듯해도
   조합이 "16시간 깨어 있다가 8시간 잔다"로 수렴하지 않으면 소용이 없다.

### 1-2. 잘 안 보였던 이유

- **조절판이 차트를 화면 밖으로 밀어냈다.** 첫 화면에 슬라이더 열 개만 보이고 그래프가 없었다.
- **파란 "수면압력" 선이 중복 정보였다.** 초록과 보라의 간격을 눈으로 이미 읽고 있는데
  그 차이를 선으로 또 그리니, 제일 굵고 진한 선이 제일 정보가 적은 선이 돼 있었다.
- **간격 채우기가 세로 막대 200개였다.** `stride 5`로 폴리곤을 쌓아서 줄무늬가 생겼다.
- **`preserveAspectRatio="none"`.** 컨테이너 폭에 맞춰 SVG를 가로로 늘려서 글자까지 늘어났다.
- **y축이 자동 스케일이었다.** 슬라이더를 만질 때마다 축이 움직여서 앞뒤를 견줄 수 없었다.
- **시간 맥락이 없었다.** 밤이 어디인지, 며칠째인지 표시가 없어 07:00 라벨 열두 개만 반복됐다.
- **값을 읽을 방법이 없었다.** 곡선 높이를 눈대중해야 했다.
- 라벨 겹침: "자동수면" 글자끼리, "수면 임계값"과 오른쪽 끝 곡선이 겹쳤다.

---

## 2. 새 모델 — Borbély 2-프로세스

### 2-1. 뼈대

- **프로세스 S(아데노신)**: 깨어 있으면 100%를 향해 지수적으로 차오르고, 자면 지수적으로 빠진다.
  - 상승 시상수 τr = **18.2시간**
  - 하강 시상수 τd = **4.2시간**
  - (Daan, Beersma & Borbély 1984)
- **프로세스 C(각성 신호)**: 몸시계가 만드는 곡선. 여기서 **잠드는 선**과 **깨는 선**이 나온다.
  - S가 잠드는 선을 위로 뚫으면 → 잠든다
  - S가 깨는 선 아래로 내려오면 → 깬다
- 주기는 **24.2시간** (Czeisler et al. 1999, 사람의 고유 주기 ≈ 24h 11min).
  24보다 길어서 그냥 두면 취침이 매일 조금씩 밀린다. 화면에서도 23:06 → 23:12 → 23:24로 밀린다.

### 2-2. 16시간 깨어 있고 8시간 자는 하루를 역산하기

두 시상수를 고정해 두면 "16/8이 스스로 반복되는" 시작·끝 값이 하나로 정해진다.

```
자는 동안 8시간:   S_기상 = S_취침 · e^(-8/4.2)  = 0.1497 · S_취침
깨어 있는 16시간:  S_취침 = 1 - (1 - S_기상) · e^(-16/18.2)
```

연립하면

```
S_취침 = 0.624   (62%)   ← 잠드는 순간
S_기상 = 0.093   ( 9%)   ← 깨는 순간
```

그래서 슬라이더 "아침에 남아 있던 아데노신"의 기본값이 **9%**다.
임의로 고른 숫자가 아니라 위 두 식의 고정점이다.

### 2-3. 각성 신호의 파형 — 순수 sin을 버린 이유

Daan·Beersma·Borbély가 쓴 찌그러진 sin을 그대로 가져왔다.

```
w(x) = 0.97·sin(x) + 0.22·sin(2x) + 0.07·sin(3x) + 0.03·sin(4x) + 0.001·sin(5x)
```

이 파형을 **부호를 뒤집어** 각성 신호로 쓴다(원래는 잠이 오는 정도라 위아래가 반대다).
뒤집은 뒤 수치로 확인한 성질:

- 최저점에서 최고점까지 **16시간**, 최고점에서 다시 최저점까지 **8시간**
- 즉 새벽에 바닥을 치고 저녁까지 완만히 오르다 밤에 가파르게 떨어진다

순수 sin은 정점과 저점이 반드시 12시간 간격이라 이걸 못 만든다.
**"하루 종일 아데노신이 쌓였는데 저녁 9시엔 오히려 정신이 드는" 현상이 이 비대칭에서 나온다.**
시각화 03번 설명 카드가 이 얘기다.

최저점 위상은 x = 1.0452 rad, 정규화 나눗수는 1.00374(파형 진폭을 ±1로 맞추는 값).
기본 최저 시각은 **05:00**(심부 체온 최저점 무렵)으로 잡았고, 그러면 정점은 자동으로 21:00이 된다.

### 2-4. 각성 신호의 크기 — 역산해서 넣은 값

여기부터는 문헌 값이 아니라 **기본값이 23:00 취침 / 07:00 기상으로 떨어지도록 맞춘 값**이다.

조건 두 개:

```
잠드는 선 = 각성 신호 + 0      →  C(23:00) = 0.624
깨는  선 = 각성 신호 - 0.20    →  C(07:00) = 0.093 + 0.20 = 0.293
```

위상을 정해 두면 그 두 시각의 파형 값이 나오므로(각각 +0.7581, −0.8997)
평균과 진폭이 하나로 풀린다.

```
진폭 A = 0.199
평균 m = 0.473        →  각성 신호 범위 27.3% ~ 67.2%
```

**"잠드는 선 높이"의 기본값을 0으로 잡은 게 이 설계의 핵심이다.**
0이면 잠드는 선이 각성 신호 곡선 그 자체가 되고, 취침 순간이
**초록 곡선이 보라 곡선을 뚫는 교차점**으로 화면에 그대로 보인다.
임의의 임계값 숫자를 설명할 필요가 없어진다.

### 2-5. 검증

파이썬으로 같은 식을 돌려 확인한 뒤 옮겼다.

```
잠듦 23:00  기상 07:00  길이 8.00h
잠듦 23:08  기상 07:11  길이 8.05h        ← 주기 24.2h라 8분 밀림
```

브라우저에서 실제로 나온 값(기본값, 사흘):

```
잠든 횟수 3번 / 평균 수면 길이 8시간 / 평균 취침 23:14 / 평균 기상 07:14
23:06 → 07:06,  23:12 → 07:12,  23:24 → 07:24
```

호버로 23:00 지점을 읽으면 `아데노신 62% · 잠드는 선 62% · 졸림 지수 100`이 나온다.
2-2에서 손으로 푼 S_취침 = 0.624와 맞는다.

---

## 3. 화면을 어떻게 고쳤나

### 3-1. 패널을 둘로 나눴다

- **위**: 아데노신 곡선 + 잠드는 선 + 깨는 선. 두 문턱 사이는 옅게 채워서
  "여기 있으면 자지도 깨지도 않는다"가 보이게 했다.
  아데노신이 잠드는 선을 넘어선 부분만 초록으로 채운다 = 넘쳐버린 수면압력.
- **아래**: 졸림 지수 하나만. 세 선을 한 판에 겹치던 걸 그만뒀다.

### 3-2. 졸림 지수라는 눈금을 새로 만들었다

원래 압력값(S − C)은 −0.2 ~ +0.07 같은 숫자라 읽어도 감이 안 온다.
두 문턱 사이를 100등분해서 **깨는 선 = 0, 잠드는 선 = 100**으로 환산했다.
"졸림 지수 100이면 잠든다"는 문장 하나로 설명이 끝난다.

범위를 벗어나는 값은 잘라내지 않고 `clipPath`로 가린다.
바닥에 붙여 놓으면 곡선이 평평해져서 "값이 멈췄다"로 잘못 읽힌다
(낮잠을 넣으면 실제로 한참 아래로 내려간다).

### 3-3. 나머지

| 문제 | 조치 |
|---|---|
| 조절판이 차트를 밀어냄 | 차트 → 요약 → 조절판 순서로 재배치 |
| 축이 슬라이더마다 움직임 | 위 패널 0~100% 고정 |
| 글자가 가로로 늘어남 | `preserveAspectRatio="none"` 제거, `height: auto` |
| 간격 채우기 줄무늬 | 폴리곤 200개 → 구간마다 닫힌 path 하나 (`areaBetween`) |
| 며칠째인지 모름 | 자정마다 날짜 경계선 + "2일차/3일차" |
| 밤이 어디인지 모름 | 22:00~06:00 배경 음영 |
| 범례를 눈으로 왕복 | 곡선 오른쪽 끝에 이름 직접 표시(겹치면 아래로 밀어냄) |
| 값을 못 읽음 | 호버 시 세로선 + 점 + 읽기 상자 |
| 그래서 몇 시간 잤는데? | 요약 4칸(횟수·평균 길이·평균 취침·평균 기상) |
| 슬라이더 라벨이 무의미 | 전부 실단위로: "0.055/h" → "18.2시간", "상대 수치" → "%" |
| 되돌릴 방법 없음 | "기본값으로" 버튼 추가 |

---

## 3.5. 두 번째 개편 — "값을 바꿔도 어디가 바뀌는지 모르겠다"

3-1~3-3으로 숫자는 맞았는데 다시 같은 지적이 왔다. 두 갈래였다.

> 값 세팅하는 것과 그래프가 한 눈에 담기질 않다보니 수치를 변경해도 어디가 변경되는지 잘 파악이 안 된다.
> 하루주기와 아데노신이 쌓인 수치 및 그에 따른 졸림 정도가 시각적으로 잘 보이지가 않는다.

### 3.5-1. 배치 — 그래프를 화면에 고정했다 (3.6에서 걷어냄)

3-3에서 "차트 → 요약 → 조절판" 순서로 바꾼 건 **첫 화면**만 고친 것이었다.
실제로 재보니 차트는 750px에서 끝나고 슬라이더는 880px에서 시작해서,
900px 높이 화면에서는 여전히 **둘 중 하나만** 보였다. 슬라이더를 만지려고 스크롤하면 그래프가 사라진다.

- `.sp-chart-pane`을 `position: sticky; top: 52px`으로 고정했다(52px = `.site-nav`의 min-height).
  고정 덩어리가 스테이지 폭을 꽉 채워야 아래 내용이 옆으로 비쳐 지나가지 않으므로,
  패딩을 `.visual-stage`에서 떼어 안쪽 두 덩어리(`.sp-chart-pane` / `.sp-control-pane`)에 옮겼다.
- 고정된 높이만큼 세로 예산이 빠듯해져서 SVG 비율을 **2.9:1로 눕혔다**(1360×470).
  이 비율은 미학이 아니라 계산 결과다 — 렌더 높이가 400px을 넘으면 슬라이더 두 줄이 화면 밖으로 밀린다.
- 제목·설명을 한 줄로 눕히고(`.sp-pane-lead`), 이 페이지의 h1을 최대 2.5rem으로 줄였다.

실측(1440×900): 고정 덩어리 577px + 조절판 385px. 제목 높이(60px)만큼 스크롤하면
**그래프·요약 6칸·슬라이더 8개가 한 화면에 모두 들어온다.**

세로가 짧은 화면(`max-height: 780px`)에서는 설명 문단을 접고, 좁은 화면(`max-width: 900px`)에서는
고정을 아예 푼다. 세로가 모자라면 고정이 오히려 방해가 된다.

### 3.5-2. 하루주기를 실제로 그렸다

이게 두 번째 지적의 핵심 원인이었다. 그동안 화면에 있던 보라 선은 **잠드는 선과 깨는 선**뿐이고,
정작 **각성 신호(프로세스 C) 본체는 그린 적이 없었다.** 몸시계라는 것이 화면 어디에도 없으니
"하루주기"가 개념으로 잡힐 리가 없었다.

- 각성 신호를 실선(2.6)으로 그리고, 문턱 두 선은 점선(1.7)으로 낮췄다.
  문턱은 각성 신호에서 위아래로 떨어뜨린 것뿐이라 실선은 하나면 된다.
- 기본값에서는 "잠드는 선 높이 = 0"이라 잠드는 선과 각성 신호가 겹친다. 이건 의도한 것이고
  (2-4 참고), 슬라이더를 올리면 두 선이 갈라지면서 관계가 드러난다.

### 3.5-3. 쌓인 양과 졸림 정도를 면적·색으로 바꿨다

선은 "값"은 보여줘도 "쌓였다"는 못 보여준다.

| 대상 | 전 | 후 |
|---|---|---|
| 아데노신 | 선 하나 | 바닥까지 채운 면적 + 잠드는 선 위 초과분은 진하게 |
| 졸림 지수 | 106px 패널의 얇은 선 | 0선 기준 면적(위=파랑, 아래=회색) + 100 위쪽 배경 구역 |
| 졸림 정도 | 없음 | 시간축 아래 **색띠** 한 줄. 진할수록 졸리다 |

색띠는 `--pressure`의 `fill-opacity`만 0.04~0.96으로 바꿔 칠한다. 새 색을 만들지 않았다.
0.5시간마다 한 칸(144칸), 칸 사이 흰 실선이 보이지 않게 0.5px씩 겹쳐 그린다.

요약도 4칸에서 6칸으로 늘렸다. **최고 졸림 지수**와 **깰 때 남은 아데노신**을 추가했는데,
둘 다 슬라이더를 만졌을 때 결과가 숫자로 즉시 돌아오게 하려는 것이다(설명 카드 03과도 이어진다).

### 3.5-4. "어디가 바뀌었는지"를 직접 표시했다

슬라이더 하나가 사흘치 곡선을 통째로 갈아끼우니, 아무 표시가 없으면 눈으로 못 쫓는다. 둘을 넣었다.

- **회색 곡선(ghost).** 슬라이더를 잡는 순간(`pointerdown`/`keydown`)의 곡선을 붙들어 두고
  회색 점선으로 겹쳐 그린다. 놓고 나서 0.9초 뒤에 사라진다.
  아데노신·잠드는 선·졸림 지수 셋만 남긴다 — 네 줄이 되면 회색이 그림을 덮는다.
- **굵기 강조(focus).** HTML의 `data-affects`(`adenosine` / `circadian` / `threshold`)를
  app.js가 읽어 `#sleepChart[data-focus]`에 넣고, CSS가 해당 곡선만 굵게 만든다.
  "이 슬라이더는 저 선을 움직인다"가 만지는 즉시 보인다.

몸시계 주기를 24.2 → 25.4시간으로 밀어 보면 회색 곡선과 새 곡선이 사흘째에 눈에 띄게 벌어지고,
요약이 23:14 → 23:56으로 따라 움직인다.

### 3.5-5. 좁은 화면 (min-width 값은 3.6에서 620px로 바뀜)

430px에서는 2.9:1 비율을 그대로 지키면 차트 높이가 130px까지 납작해진다.
폭을 줄이는 대신 `.sp-frame`을 가로 스크롤로 두고 `#sleepChart`에 `min-width: 720px`을 줬다.
`touch-action`이 `none`(강제수면 드래그용)이면 손가락으로 밀 수가 없어서 `pan-x`로 바꿨다.

---

## 3.6. 세 번째 개편 — 전체를 한 판에 그리지 않기

3.5에서 고정(sticky)으로 그래프를 화면에 붙들어 뒀지만, 근본 원인은 그대로였다.
**사흘 전체를 한 판에 그리느라 가로를 전부 써야 했고, 그래서 조절판이 아래로 밀릴 수밖에 없었다.**
비율을 2.9:1까지 눕힌 것도 그 때문이고, 하루가 300px로 눌려 곡선의 오르내림이 안 읽힌 것도 그 때문이다.

이번에는 **계산과 표시를 분리했다.** 계산은 여전히 전체 기간을 돌리고,
화면에는 '지금' 주변의 12/24/48시간만 잘라 보여준다.

### 3.6-1. 무엇이 달라졌나

| | 전 | 후 |
|---|---|---|
| 한 화면 | 72시간 고정 | 12 / 24 / 48시간 (버튼) |
| 세로 비율 | 2.9:1 (하루 = 300px) | 1.8:1 (하루 = 750px) |
| 조절판 위치 | 그래프 아래 (스크롤해야 보임) | 그래프 오른쪽 372px 열 |
| 전체 길이 | 3일 고정 | 2~10일 (슬라이더) |
| 전체 맥락 | 없어도 됐음(다 보였으니) | 미니맵 |

가로를 되찾자 조절판이 옆으로 들어갔고, 세로를 되찾자 곡선이 펴졌다. 하나를 고치니 둘이 풀렸다.

### 3.6-2. 창을 어떻게 옮기나

`viewStart = clamp(nowHour − windowHours/2, 0, totalHours − windowHours)`.
'지금'을 가운데 두되 양 끝에서는 창을 고정하고 표시만 움직인다. 클램프를 안 하면
처음과 끝에 빈 공간이 생긴다.

시간을 옮기는 길은 넷이다. **지금 시각 슬라이더**, **미니맵 드래그**, **재생 버튼**,
그리고 강제수면 모드가 꺼져 있을 때의 그래프 호버(값 읽기만).

`x(hour)`가 모듈 스코프의 `viewStart`를 읽도록 해서, 창이 바뀌어도 그리는 함수들의 시그니처는
그대로 뒀다. 점은 `visibleSlice()`로 창 안것만 넘긴다 — 전체를 path로 만들면 대부분이 화면 밖 좌표다.
양쪽으로 한 점씩 더 떼고 `clipPath`로 자른다. 안 그러면 곡선이 창 가장자리에 못 닿는다.

### 3.6-3. 재생

`requestAnimationFrame`으로 1.5시간/초씩 전진한다. 두 가지를 조심해야 했다.

- **기준 시각은 첫 프레임에서 잡는다.** `startPlaying`에서 `performance.now()`로 미리 잡아 뒀더니
  첫 걸음이 23:00 → 22:16으로 **뒤로** 뛰었다. rAF가 넘겨주는 stamp와 `performance.now()`가
  서로 다른 시계일 수 있어서다(iframe에서 특히). 둘 다 rAF stamp로 통일해야 한다.
- **dt 클램프 0.25초.** 탭이 잠깐 멈췄다 돌아오면 한 프레임에 몇 시간을 건너뛴다.
  CLAUDE.md가 캔버스 루프에 걸어 두라고 한 그 클램프다.

`document.hidden`이면 재생을 멈춘다. 안 그러면 돌아왔을 때 시간이 엉뚱한 데 가 있다.

### 3.6-4. 다시 계산하지 않기

재생 중에는 매 프레임 다시 그리는데, 시간만 흐르고 곡선은 그대로다.
설정과 강제수면과 일수를 키로 삼아 시뮬레이션 결과를 캐시한다(`getSimulation`).
없으면 초당 60번씩 전체 기간을 헛계산한다.

### 3.6-5. 며칠이든 되게

전체를 한 판에 그리던 때는 일수를 늘리는 순간 하루가 200px로 눌려서 늘릴 수가 없었다.
창을 잘라 보게 되니 제약이 사라져서 **2~10일** 슬라이더를 넣었다.
`totalHours`를 `const`에서 `let`으로 바꾸고, 날짜 경계·밤 띠·미니맵 라벨·시간 슬라이더 최대값이
전부 `state.days`를 따라가게 했다.

열흘로 켜면 미니맵의 하루 폭이 100px 아래로 떨어져 날짜 라벨이 겹친다. 그래서 하루 폭이
74px 미만이면 라벨을 접는다. 미니맵 곡선의 점도 전체 길이에 비례해 솎는다(항상 300점 안팎).

주기 24.2시간의 효과가 이때 제대로 드러난다. 사흘이면 평균 취침이 23:14인데,
이레로 늘리면 23:37이 된다. 하루 8분씩 밀리는 게 누적된 결과다.

### 3.6-6. 숫자 패널을 둘로 나눴다

- **지금 줄** (그래프 바로 아래): 시각 · 상태 · 아데노신 · 각성 신호 · 졸림 지수 · 다음 변화.
  '다음 변화'는 `잠들기까지 3시간 20분`처럼 다음 전환까지 남은 시간이다.
  슬라이더를 만졌을 때 "그래서 언제 자는데?"에 바로 답한다.
- **전체 요약**: 잠든 횟수 · 평균 길이 · 평균 취침 · 평균 기상 · 최고 졸림 · 깰 때 잔여.

요약을 조절판 쪽에 뒀더니 오른쪽 열이 왼쪽보다 275px 길어져 화면 밖으로 밀렸다.
그래프 아래로 옮기니 양쪽이 690 / 738로 맞았다(1440×900에서 둘 다 한 화면에 들어온다).

### 3.6-7. shared.js: "0시간 6분"

'다음 변화'가 `잠들기까지 0시간 6분`으로 나왔다. `formatHours`가 한 시간 미만일 때
`0시간`을 붙이고 있었다. 분만 적도록 고쳤다. **`amyloid` `alcohol` `light` `microsleep`도
같이 쓰는 함수라 넷 다 좋아지는 수정이다** (4-1과 같은 함수, 같은 성격의 버그).

---

## 4. 작업하다 걸린 것들

### 4-1. `formatHours`가 "7시간 60분"을 뱉었다 (shared.js)

`Math.floor(7.997)` = 7, `Math.round(0.997·60)` = 60. 분 단위 반올림이 60이 되는데 시간으로 안 올렸다.
총 분으로 바꿔 계산하도록 고쳤다. **`amyloid` `alcohol` `light` `microsleep`도 같이 쓰는 함수라 넷 다 영향받던 버그다.**

### 4-2. 마지막 밤이 잘려 길이가 거짓말을 했다

72시간 끝에서 잘린 수면 구간에 "7시간 36분"이라고 찍혔다.
**계산은 화면보다 14시간 길게 돌리고(`simHours`), 그리기만 72시간에서 자른다.**
덕분에 사흘째 밤도 온전히 세어져서 요약이 "3번 / 8시간"으로 맞는다.

### 4-3. 평균 취침 시각의 자정 문제

23:50과 00:10의 산술평균은 12:00이 된다.
시각을 각도로 바꿔 평균 낸 뒤 되돌리는 방식(`averageClock`)으로 처리했다.

### 4-4. iframe 안의 `const`는 `contentWindow`에 없다

호버 상태를 넣은 스크린샷을 찍으려고 harness에서 `w.state`를 건드렸더니 undefined였다.
`const`/`let` 최상위 선언은 window 프로퍼티가 안 된다. `w.eval('state.hoverHour = 40; drawChart();')`로 해결.

### 4-5. 브라우저 패널 캐시 (CLAUDE.md에 이미 적힌 함정)

`?v=` 쿼리 없이 접근했다가 옛 `index.html`이 떠서 `#riseTau`가 null이라는 오류를 봤다.
페이지를 **먼저** 새 쿼리스트링으로 다시 열어야 한다. `fetch(cache:'reload')`는 이미 로드된 페이지를 안 고친다.

---

## 5. 남은 것

- 각성 신호의 평균·진폭(0.473 / 0.199)은 문헌 값이 아니라 역산 값이다.
  화면 하단과 이 문서에 밝혀 두었지만, 원문헌의 문턱 값(H↑ ≈ 0.6, H↓ ≈ 0.17)과
  직접 대응시키는 편이 더 정직할 수 있다.
- 다음 시각화가 "카페인의 영향"이다. 카페인은 아데노신 수용체를 막는 것이지
  아데노신 자체를 줄이지 않으므로, **아데노신 곡선은 그대로 두고 잠드는 선을 밀어 올리는** 방식이
  이 모델과 자연스럽게 붙는다. 지금 구조 그대로 확장 가능하다.

## 참고

- Borbély, A. A. (1982). *A two process model of sleep regulation.* Human Neurobiology, 1, 195–204.
- Daan, S., Beersma, D. G., & Borbély, A. A. (1984). *Timing of human sleep: recovery process gated by a circadian pacemaker.* American Journal of Physiology, 246, R161–R183.
- Czeisler, C. A. et al. (1999). *Stability, precision, and near-24-hour period of the human circadian pacemaker.* Science, 284, 2177–2181.
- Dijk, D. J., & Czeisler, C. A. (1994). 저녁의 각성 유지 구간(wake maintenance zone)에 관한 근거.

### 4-6. headless 스크린샷의 최소 창 너비

430px 모바일을 확인하려고 `--window-size=430,1900`으로 찍었더니 슬라이더 카드가
오른쪽으로 잘려 나온 그림이 나왔다. 레이아웃이 깨진 줄 알았는데 아니었다.
macOS Chrome은 창 너비에 하한이 있어서 **실제 뷰포트는 430보다 넓게 잡히고 스크린샷만 430으로 잘린다.**
같은 페이지를 430px iframe에 넣고 재보니 `documentElement.scrollWidth === 430`, 넘치는 요소는 없었다.

CLAUDE.md의 "스크린샷만 근거로 판단하지 말 것"이 그대로 적용된다.
좁은 폭을 볼 때는 **430px `<iframe>`을 넉넉한 창에 띄우고** 그 안을 찍는다.
같은 harness로 `scrollTo`·이벤트 주입도 되므로 sticky 동작과 슬라이더 드래그 검증에도 쓴다.

### 4-7. headless는 rAF를 초당 한두 개밖에 안 준다

재생 기능을 검증하려는데 4초를 기다려도 시간이 안 움직였다. 코드 버그인 줄 알았지만
`requestAnimationFrame` 카운터를 붙여 보니 **1초에 프레임이 1~2개**였다.
`--dump-dom` 모드에서는 합성을 안 해서 아예 0개인 구간도 있고, iframe 안이면 더 심하다.

**How to apply:** rAF 루프는 시간을 기다려 확인하지 말고 **프레임 시계를 직접 돌린다.**
harness에서 iframe의 `requestAnimationFrame`을 큐에 쌓는 가짜로 바꾸고, 원하는 stamp로 직접 호출한다.

```js
let queue = [];
w.requestAnimationFrame = (cb) => queue.push(cb);
w.cancelAnimationFrame = () => { queue = []; };
play.click();
let stamp = 5000;
for (let i = 0; i < 61; i += 1) { stamp += 16.667; const due = queue; queue = []; due.forEach((cb) => cb(stamp)); }
// state.nowHour가 정확히 1.5시간 나아갔는지 본다
```

이렇게 하니 "1초에 1.5시간", "3초 멈췄다 돌아오면 dt 클램프로 0.375시간만", "끝에서 자동 정지"를
전부 결정적으로 확인할 수 있었다. 상태는 4-4대로 `w.eval("state.nowHour")`로 읽는다.
